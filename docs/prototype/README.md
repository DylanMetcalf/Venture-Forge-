# Prototype (reference only)

`venture-forge.html` is the original single-file prototype built inside a Claude.ai
artifact, and `HANDOFF.md` is the brief that accompanied it. They are kept here
unchanged as a product and visual reference. The production app in `src/` replaced
them; nothing imports from this folder.
